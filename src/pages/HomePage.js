import { SearchForm } from "../components/SearchForm";

export function HomePage() {
    return (
        <main>
            <section>
                <h1>Find your next vacation</h1>
                <SearchForm />
            </section>
        </main>
    );
};